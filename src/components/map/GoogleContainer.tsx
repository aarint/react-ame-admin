import React from 'react';

export default class GoogleContainer extends React.Component {
  render() {
    return (
      <div style={{ padding: 20, background: '#fff' }}>
        Google Map Visual (placeholder – add Google Maps SDK to enable)
      </div>
    );
  }
}
